# Playground

AI-powered project playground

## Web App

[`web/`](web/) contains an installable PWA voice-recording frontend that
streams audio to a backend in chunks via the `MediaRecorder` API. See
[`web/README.md`](web/README.md) for details on running it locally and
wiring it up to a backend.
