# Copy to .env and fill in real values before running `docker compose up -d`.
# Generate secrets with: openssl rand -base64 60 | tr -d '\n'

PG_USER=authentik
PG_DB=authentik
PG_PASS=

AUTHENTIK_SECRET_KEY=
